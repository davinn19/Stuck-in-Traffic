extends Area2D

func _physics_process(delta : float):
#	for car in get_overlapping_bodies():
#		if car is Car:
#			car.terrain_modifier = pow(car.velocity, 3)
#			if car.health > 0:
#				car.health -= delta
	pass
